{
  "name": "agent-ide",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.6.0",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check": "pnpm typecheck",
    "dev": "node -e \"console.log('Scaffold created. Next milestone: wire the runtime graph and persistence.');\""
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "mysql2": "^3.20.0",
    "redis": "^5.11.0",
    "zod": "^4.3.6"
  }
}
